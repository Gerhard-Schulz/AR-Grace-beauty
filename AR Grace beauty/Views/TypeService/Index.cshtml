@model IEnumerable<TypeService>
@{
    ViewData["Title"] = "Услуги";
}

<table class="table table-hover w-100 mt-3 pb-2 cell-centered rounded-corners">
    <thead class="table-dark"> 
        <tr>
            <th colspan="6">
                <h1 class="text-center text-light mt-1">Услуги по видам</h1>
            </th>
        </tr>
        <tr>
            <th>Услуга</th>
            <th>Вид услуги</th>
            <th>Цена за вид</th>
            <th>Итоговая цена</th>
            <th>Опиание</th>
            <th></th>
        </tr>
    </thead>
    <tbody class="table-primary">
        @foreach (var typeService in Model)
        {
            <tr>
                <td>
                    @typeService.Service.Name
                </td>
                <td>
                    @typeService.Name
                </td>
                <td>
                    @typeService.Price руб.
                </td>
                <td>
                    @typeService.TotalPrice руб.
                </td>
                <td>
                    @typeService.Description
                </td>
                <td>
                    <div class="btn-group w-100" role="group">
                        <a asp-controller="TypeService" asp-action="Edit" asp-route-id="@typeService.Id" class="btn btn-warning"><i class="bi bi-pen"></i> Изменить</a>
                        <a asp-controller="TypeService" asp-action="Delete" asp-route-id="@typeService.Id" class="btn btn-primary"><i class="bi bi-trash3"></i> Удалить</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
