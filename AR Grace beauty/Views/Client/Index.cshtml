﻿@using AR_Grace_beauty.Models;
@model IEnumerable<Client>

@{
    ViewData["Title"] = "Наши клиенты";
}

<table class="table table-hover w-100 mt-3 pb-2 cell-centered rounded-corners">
    <thead class="table-primary"> 
        <tr>
            <th colspan="3">
                <h2 class="text-center text-light mt-1">Клиенты</h2>
            </th>
        </tr>
    </thead>
    <tbody class="table-secondary">
        @foreach (var client in Model)
        {
            <tr>
                <td>
                    @client.Name
                </td>
                <td>
                    @client.Phone
                </td>
                <td>
                    <div class="btn-group w-100" role="group">
                        <a asp-controller="Client" asp-action="Edit" asp-route-id="@client.Id" class="btn btn-warning"><i class="bi bi-pen"></i> Edit</a>
                        <a asp-controller="Client" asp-action="Delete" asp-route-id="@client.Id" class="btn btn-danger"><i class="bi bi-trash3"></i> Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
