﻿@using AR_Grace_beauty.Models;
@model IEnumerable<Service>

@{
    ViewData["Title"] = "Услуги";
}

<table class="table table-hover w-100 mt-3 pb-2 cell-centered rounded-corners">
    <thead class="table-primary"> 
        <tr>
            <th colspan="3">
                <h2 class="text-center text-light mt-1">Услуги</h2>
            </th>
        </tr>
    </thead>
    <tbody class="table-secondary">
        @foreach (var service in Model)
        {
            <tr>
                <td>
                    @service.Name
                </td>
                <td>
                    @service.Price руб.
                </td>
                <td>
                    <div class="btn-group w-100" role="group">
                        <a asp-controller="Service" asp-action="Edit" asp-route-id="@service.Id" class="btn btn-warning"><i class="bi bi-pen"></i> Edit</a>
                        <a asp-controller="Service" asp-action="Delete" asp-route-id="@service.Id" class="btn btn-danger"><i class="bi bi-trash3"></i> Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
